# Sample Static Allocation Strategy

Reference Virtuoso app for Maestro v0.1.

This package is intentionally separate from Maestro core. It imports only from
`maestro.sdk` and can be loaded through:

```yaml
entrypoint: "sample_static_allocation.strategy:SampleStaticAllocationStrategy"
```
