MAESTRO_CONFIG=configs/paper.yaml
TELEGRAM_BOT_TOKEN=replace-with-telegram-bot-token
KIS_ACCOUNT_ID=replace-with-kis-account-id
KIS_APP_KEY=replace-with-kis-app-key
KIS_APP_SECRET=replace-with-kis-app-secret
# Optional: set only when using a real pre-issued access token.
# KIS_ACCESS_TOKEN=
# Optional: set only when using a real pre-issued WebSocket approval key.
# KIS_APPROVAL_KEY=
