[Unit]
Description=Maestro read-only Dashboard
Documentation=file:/home/symphony/maestro/docs/vps_systemd.md
After=network-online.target
Wants=network-online.target
# Never give up restarting: keep the dashboard available even after repeated
# crashes (default systemd rate-limiting would stop it after a few failures).
StartLimitIntervalSec=0

[Service]
Type=simple
User=symphony
Group=symphony
UMask=0077
WorkingDirectory=/home/symphony/maestro
EnvironmentFile=/etc/maestro/maestro.env
ExecStart=/home/symphony/maestro/.venv/bin/maestro dashboard --config ${MAESTRO_READONLY_CONFIG} --signal-config ${MAESTRO_SIGNAL_CONFIG} --host 127.0.0.1 --port 8503
Restart=always
RestartSec=5
KillSignal=SIGINT
TimeoutStopSec=20
StandardOutput=journal
StandardError=journal

[Install]
WantedBy=multi-user.target
